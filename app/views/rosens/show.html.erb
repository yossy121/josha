<h1><%= @Rosen.name %> （<%= @Rosen.start_station.name %>～<%= @Rosen.end_station.name %>）駅間詳細</h1>
<table border="1">
  <col span="2" width="200" />
  <col span="1" width="75" />
  <tbody>
    <tr>
      <th>開始駅</th>
      <th>終了駅</th>
      <th>営業キロ</th>
    </tr>
    <% @Sections.each do |s| %>
    <tr>
      <td><%= s.start_station.name %></td>
      <td><%= s.end_station.name %></td>
      <td><%= s.kilo %></td>
   </tr>
   <% end %>
  </tbody>
</table>
